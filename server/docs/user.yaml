components:
  schemas:
  requestBodies:
    Auth:
      description: Signup, login
      required: true
      content:
          application/json:
            schema:
              type: object
              properties:
                email:
                  type: string
                password:
                  type: string
              required:
                - email
                - password